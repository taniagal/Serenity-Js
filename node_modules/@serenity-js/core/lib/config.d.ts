export declare class Config<T> {
    private config;
    constructor(config: T);
    readonly get: T;
    mergedWith(otherConfig: T): Config<T>;
    withFallback(defaults: T): Config<T>;
    private mergeValuesAndConcatenateLists;
    private mergeValuesButOverrideLists;
}
