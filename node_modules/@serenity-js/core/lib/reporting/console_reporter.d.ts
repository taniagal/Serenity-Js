import { DomainEvent } from '../domain/events';
import { Stage, StageCrewMember } from '../stage';
export declare type MessagePrinter = (message: string) => void;
export declare function consoleReporter(print?: MessagePrinter): StageCrewMember;
export declare class ConsoleReporter implements StageCrewMember {
    private print;
    private static Events_of_Interest;
    private stage;
    constructor(print: MessagePrinter);
    assignTo(stage: Stage): void;
    notifyOf(event: DomainEvent<any>): void;
}
