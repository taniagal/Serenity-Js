/// <reference types="node" />
import * as gracefulFs from 'graceful-fs';
export declare class FileSystem {
    private readonly root;
    private readonly fs;
    constructor(root: string, fs?: typeof gracefulFs);
    store(relativePathToFile: string, data: any): Promise<string>;
    private ensureSpecified(relativePathToFile);
    private prepareDirectory(relativePathToFile);
    private write(path, data);
}
