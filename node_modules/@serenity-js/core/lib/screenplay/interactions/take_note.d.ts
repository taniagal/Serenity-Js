import { Interaction, Question, UsesAbilities } from '../index';
export declare class TakeNote<T> implements Interaction {
    private question;
    private topic;
    static of: <Answer>(question: Question<Answer>) => TakeNote<Answer>;
    as: (topic: string) => this;
    performAs(actor: UsesAbilities): PromiseLike<void>;
    constructor(question: Question<T>, topic: {
        toString: () => string;
    });
    toString: () => string;
    private subject;
}
