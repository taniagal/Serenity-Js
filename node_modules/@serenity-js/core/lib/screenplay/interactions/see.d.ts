import { AnswersQuestions, Interaction, Question } from '..';
import { Assertion } from '../expectations';
export declare class See<S> implements Interaction {
    private question;
    private assert;
    static if: <T>(question: Question<T>, assertion: Assertion<T>) => See<T>;
    performAs(actor: AnswersQuestions): PromiseLike<void>;
    constructor(question: Question<S>, assert: Assertion<S>);
    toString: () => string;
}
