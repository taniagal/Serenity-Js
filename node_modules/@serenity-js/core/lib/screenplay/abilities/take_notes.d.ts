import { Ability, UsesAbilities } from '../actor';
export interface Notepad {
    [x: string]: PromiseLike<any>;
}
export declare class TakeNotes implements Ability {
    private notepad;
    static usingAnEmptyNotepad: () => TakeNotes;
    static using: (notepad: Notepad) => TakeNotes;
    static as(actor: UsesAbilities): TakeNotes;
    note: (topic: string, contents: any) => Promise<void>;
    read(topic: string): PromiseLike<any>;
    constructor(notepad: Notepad);
}
