export declare function defer<T>(fn: () => PromiseLike<T>): Promise<T>;
export declare class Deferred<T> {
    promise: Promise<T>;
    resolve: (T?) => void;
    reject: (Error) => void;
    constructor();
}
