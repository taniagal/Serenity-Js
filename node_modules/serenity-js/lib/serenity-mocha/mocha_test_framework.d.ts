import { TestFrameworkAdapter } from '../serenity-protractor/framework';
import { MochaConfig } from './mocha_config';
export declare class MochaTestFramework implements TestFrameworkAdapter {
    private config;
    private mocha;
    constructor(config: MochaConfig);
    run(specs: string[]): PromiseLike<any>;
    private stageCue;
    private registerCompilerIfNeeded(compiler);
}
