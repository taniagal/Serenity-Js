import { DomainEvent } from '@serenity-js/core/lib/domain';
import { Stage, StageCrewMember } from '@serenity-js/core/lib/stage';
import { ProtractorBrowser } from 'protractor';
export declare class ProtractorBrowserDetector implements StageCrewMember {
    private browser;
    private static Events_of_Interest;
    private stage;
    constructor(browser: ProtractorBrowser);
    assignTo(stage: Stage): void;
    notifyOf(event: DomainEvent<any>): void;
    private sceneStarts(event);
}
