import { Question, UsesAbilities } from '@serenity-js/core/lib/screenplay';
import { Target } from '../ui/target';
import { ElementFinder } from 'protractor';
export declare class WebElement implements Question<ElementFinder> {
    private target;
    static of(target: Target): WebElement;
    answeredBy(actor: UsesAbilities): ElementFinder;
    constructor(target: Target);
    toString: () => string;
}
