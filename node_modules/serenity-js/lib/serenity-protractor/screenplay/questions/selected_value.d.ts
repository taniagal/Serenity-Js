import { Question, UsesAbilities } from '@serenity-js/core/lib/screenplay';
import { Target } from '../ui/target';
export declare class SelectedValue implements Question<PromiseLike<string>> {
    private target;
    static of: (target: Target) => SelectedValue;
    answeredBy(actor: UsesAbilities): PromiseLike<string>;
    constructor(target: Target);
    toString: () => string;
}
export declare class SelectedValues implements Question<PromiseLike<string>> {
    private target;
    static of(target: Target): SelectedValues;
    answeredBy(actor: UsesAbilities): PromiseLike<string>;
    constructor(target: Target);
    toString: () => string;
}
