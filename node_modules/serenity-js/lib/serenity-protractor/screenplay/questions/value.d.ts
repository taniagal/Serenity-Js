import { Question, UsesAbilities } from '@serenity-js/core/lib/screenplay';
import { Target } from '../ui/target';
export declare class Value implements Question<PromiseLike<string>> {
    private target;
    static of: (target: Target) => Value;
    answeredBy(actor: UsesAbilities): PromiseLike<string>;
    constructor(target: Target);
    toString: () => string;
}
