import { PerformsTasks, Task, UsesAbilities } from '@serenity-js/core/lib/screenplay';
import { Target } from '../ui/target';
export declare class Enter implements Task {
    private value;
    private target;
    private interactions;
    static theValue(value: string | number): Enter;
    into(field: Target): Enter;
    thenHit(key: string): this;
    performAs(actor: PerformsTasks & UsesAbilities): PromiseLike<void>;
    constructor(value: string | number);
}
